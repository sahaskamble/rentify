interface:
  display_name: "Rentify"
  short_description: "Repo-specific patterns and workflows for rentify"
  default_prompt: "Use the rentify repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true